quarkus.application.name=gateway
quarkus.http.port=${HTTP_PORT:8080}

# Downstream microservices (REST clients).
# Renseigner l'URL par configKey une fois les @RegisterRestClient ajoutes.
# quarkus.rest-client.auth-api.url=${AUTH_SERVICE_URL:http://localhost:8081}
# quarkus.rest-client.jobs-api.url=${JOBS_SERVICE_URL:http://localhost:8082}
